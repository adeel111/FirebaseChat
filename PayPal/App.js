import React, {Component} from 'react';
import ByWeb from './screens/ByWeb'; // done
import ByLib from './screens/ByLib'; // pending (no need at the moment)
import CheckoutCards from './screens/CheckoutCards'; // done
import Multibanco from './screens/Multibanco'; // doing...

class App extends Component {
  render() {
    return <Multibanco />;
  }
}

export default App;
